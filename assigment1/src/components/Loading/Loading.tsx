import LoadingIcon from "../../icons/LoadingIcon";
import { LoadingWrapper } from "../LoadingError/StyledLoadingError";

export default function Loading() {
  return (
    <LoadingWrapper>
      <LoadingIcon />
    </LoadingWrapper>
  );
}
